name: randcolor
description: Change color test application.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^4.0.0
  equatable: ^1.0.0


  cupertino_icons: ^0.1.3

dev_dependencies:
  test: ^1.3.0
  bloc_test: ^5.1.0
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

